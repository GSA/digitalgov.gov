
jQuery(document).ready(function($) {
	var options = {
	  strings: ['hand-made cupcakes', 'artisnal cheese!', 'plastics!', 'a strong cup of coffee', 'dragons'],
		startDelay: 1200,
		typeSpeed: 50,
		backSpeed: 40,
		backDelay: 1600,
		loop: true,
  	loopCount: Infinity,
		smartBackspace: true // Default value
	}

	// var typed = new Typed(".tagline span", options);

});
